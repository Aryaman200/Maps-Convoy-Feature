# ============================================================
# CONVOY SERVER — ENVIRONMENT
# ============================================================
# Copy to ".env" and adjust as needed. The server also reads
# plain process env vars, so these can be set by your host.
# ============================================================

# Port the HTTP + Socket.io server listens on.
PORT=3001
